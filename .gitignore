.idea/
api/uploads/

# Local environment files (use .env.example as a template)
.env
.env.local
*.env
!.env.example

# Nix / devenv (devenv.lock is committed on purpose: it pins the toolchain)
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# macOS
.DS_Store
