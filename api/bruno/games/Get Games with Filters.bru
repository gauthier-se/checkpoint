meta {
  name: Get Games with Filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/games
  body: none
  auth: none
}

params:query {
  ~page: 0
  ~size: 20
  ~sort: releaseDate,desc
  ~genre: RPG
  ~genre: Action
  ~platform: PC
  ~yearMin: 2020
  ~yearMax: 2023
  ~ratingMin: 4.0
  ~ratingMax: 5.0
}

docs {
  # Get Games with Filters

  Retrieves a paginated list of games with optional filters.

  ## Filter Parameters

  | Parameter | Type | Description |
  |-----------|------|-------------|
  | genre | String (repeatable) | Genre name(s), case-insensitive. Multiple values match any (OR). |
  | platform | String (repeatable) | Platform name(s), case-insensitive. Multiple values match any (OR). |
  | yearMin | Integer | Minimum release year (inclusive) |
  | yearMax | Integer | Maximum release year (inclusive) |
  | ratingMin | Double | Minimum average rating (inclusive) |
  | ratingMax | Double | Maximum average rating (inclusive) |

  All filter parameters are optional and can be combined. Multiple values of the same
  facet (e.g. several genres) are combined with OR; different facets are combined with AND.

  ## Examples

  - `?genre=RPG` — Only RPG games
  - `?genre=RPG&genre=Action` — Games that are RPG OR Action
  - `?platform=PC&yearMin=2020` — PC games from 2020 onwards
  - `?ratingMin=4.0&ratingMax=5.0&sort=rating,desc` — Top rated games
  - `?genre=Action&yearMin=2020&yearMax=2023&platform=PlayStation 5` — Combined filters
}
